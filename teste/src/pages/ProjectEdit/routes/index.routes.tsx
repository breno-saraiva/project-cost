import { RouteObject } from "react-router-dom";
import ProjectEdit from "..";

const projectEdit: RouteObject[] = [
  {
    path: "/projectEdit",
    element: <ProjectEdit />,
  },
];

export { projectEdit };
